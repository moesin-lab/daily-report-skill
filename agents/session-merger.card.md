## session merged-<GROUP_ID>

- **merged_from**:
  - `<原 session_id 1>`
  - `<原 session_id 2>`
  - ...
- **工作类型**: <修Bug | 新功能 | 治理 | 调研 | 工具 | 其他>
- **状态**: <已交付 | 在分支 | 调研中 | 阻塞 | 无需交付>
- **聚类锚点**:
  - repo: `<合并后整体的主仓库>`
  - branch_or_pr: `<合并后整体的主 PR/分支；没有就填 null>`
  - issue_or_bug: `<合并后整体的核心 issue/bug 签名；没有就填 null>`
  - files:
    - `<合并后整体涉及的关键文件 1>`
    - `<合并后整体涉及的关键文件 2>`
  - target_object: `<合并后整体的操作主对象>`
- **关联事件**: 无

### 事件摘要

<3-6 行中文段落，作为一条主题工作整体描述。必须呈现跨 session 的推进脉络（V1→V2→V3 之类），不是 N 条独立摘要拼接>

### 认知增量

<基于整体主题工作的认知增量重新评估。局部无、整体有教训时写清楚；整体无就写「无」>

### 残留问题

<基于整体主题工作的残留问题汇总。没有就写「无」>

<!--
占位符纪律：
- merged_from 必填，列全被合并 session 的 session_id（主 agent 用它替换原始卡片数组里的对应单卡片）
- 关联事件固定填「无」——合并只进行一次，不再参与下一轮聚类
- 三个 H3 字段是散文，中文符号随便用
- files 子列表最多 5 条；没有文件时下面写 `- (无)`
- 输出本身是纯 Markdown，不要外层代码块、不要前后解释
-->

---

## 失败降级模板

读不到文件、窗口内无消息、其他致命错误时用这份最小合法合并卡片：

## session merged-<GROUP_ID>

- **merged_from**:
  - `<原 session_id 1>`
  - `<原 session_id 2>`
- **工作类型**: 其他
- **状态**: 无需交付
- **聚类锚点**:
  - repo: null
  - branch_or_pr: null
  - issue_or_bug: null
  - files:
    - (无)
  - target_object: null
- **关联事件**: 无

### 事件摘要

合并评估失败，原因：<简述>。

### 认知增量

无

### 残留问题

无

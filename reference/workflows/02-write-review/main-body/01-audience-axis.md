## 读者定位

读者是作者本人（即 `PERSONA.md` 或 `AUTHOR_AGENT_NAME` 所指向的身份）。日报价值是半年后回看的索引和认知沉淀，不是背景说明文。

写法：

- 少复述过程。
- 保留关键判断、状态、残留风险。
- PR 号、SHA、文件路径只在必要时出现。

## 开篇主轴

有 session cards 时，在概览后写一行：

```markdown
**今天最重要的是：...**
```

选择标准：

- 认知增量高。
- 状态接近已交付。
- 能代表当天主线。
- ≤ 60 字。

空 session 时省略。
